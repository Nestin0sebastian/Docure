<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <!-- Add Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <!-- Your custom CSS can go here if needed -->
    <style>
        @keyframes glowFade {
            0% {
                color: #23db2a; /* Start with green glow */
                text-shadow: 0 0 10px #00ff08; /* Add shadow to create glow effect */
            }
            50% {
                color: #3c3939; /* Fade to white */
                text-shadow: none; /* Remove shadow */
            }
            100% {
                color: #00ff08; /* Return to green */
                text-shadow: 0 0 10px #000000; /* Add shadow again */
            }
        }

        #companyName {
            font-size: 24px;
            text-align: center;
            margin-top: 20px;
            animation: glowFade 3s infinite; /* Apply the glowFade animation */
        }

        #bgg {
            background-color: #121212;
        }

        .card {
            background-color: #1f1f1f;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

        .card-header {
            background-color: #212121;
            border-bottom: none;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            padding: 20px;
        }

        .form-control {
            background-color: #333;
            color: #fff;
            border: 1px solid #555;
        }

        .form-control:focus {
            border-color: #aaa;
            box-shadow: none;
        }

        .btn-primary {
            background-color: #4caf50;
            border-color: #4caf50;
        }

        .btn-primary:hover {
            background-color: #388e3c;
            border-color: #388e3c;
        }

        .btn-link {
            color: #4caf50;
        }
    </style>
</head>
<body id="bgg">

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 animated">
            <div class="card">
                <div class="card-header">
                    <h3 class="text-white mb-0">Login</h3>
                </div>
                <div class="card-body">
                    <form action="/loginpost" method="post">
                        <div class="form-group">
                            <label for="email" class="text-white">Email</label>
                            <input type="email" id="email" class="form-control" name="email">
                        </div>
                        <div class="form-group">
                            <label for="password" class="text-white">Password</label>
                            <input type="password" id="password" class="form-control" name="password">
                        </div>
                        <button type="submit" class="btn btn-primary btn-block">Login</button>
                    </form>
                    <div class="text-center mt-3">
                        <a href="/signup" class="btn btn-link">Create Account</a>
                        <span class="text-white mx-2">|</span>
                        <a href="/forgotpassword" class="btn btn-link">Forgot Password</a>
                    </div>
                </div>
            </div>
            <!-- Add the company name with the specified ID -->
            <h2 id="companyName">Dolt Consultations<p style="color: white;">Ltd.</p></h2>
        </div>
    </div>
</div>

<!-- Add Bootstrap JS and jQuery (optional) -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
